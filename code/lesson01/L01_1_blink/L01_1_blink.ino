/* 第一课 · 实验 1：点亮一个 LED
 *
 * 接线：D8 --[220Ω]-- LED长脚(正极) ... LED短脚(负极) -- GND
 *      LED 有极性，接反了不亮（但不会坏）。长脚是正极。
 *
 * 你要理解的不是"灯会闪"，而是：
 *   引脚输出的是 0V 或 5V 两个状态，它是"信号"，不是可调的电源。
 */

const uint8_t LED = 8;

void setup() {
  pinMode(LED, OUTPUT);        // 告诉 MCU：这个引脚我要用来输出
}

void loop() {
  digitalWrite(LED, HIGH);     // 引脚 = 5V
  delay(500);
  digitalWrite(LED, LOW);      // 引脚 = 0V
  delay(500);
}

/* 【动手测量】拿万用表（直流电压档）测：
 *   1) LED 亮时，D8 对 GND 的电压 = ______ V   （应≈5V）
 *   2) LED 亮时，电阻两端的电压  = ______ V   （应≈3V）
 *   3) LED 亮时，LED 两端的电压  = ______ V   （应≈2V，这叫"正向压降"）
 *
 * 【算一遍】流过 LED 的电流 = 电阻两端电压 ÷ 电阻值 = 3.0V ÷ 220Ω ≈ 13.6 mA
 *   Arduino 单引脚上限约 20mA。13.6 < 20，安全。
 *
 * 【想一想】如果去掉这个 220Ω 电阻会怎样？
 *   电流 ≈ (5V - 2V) ÷ (导线电阻≈0Ω) → 趋于无穷 → 烧 LED 或烧引脚。
 *   电阻的作用不是"降压"，是"限流"。
 *
 * 这就是为什么后面电磁铁（要 500mA）绝对不能直接接引脚——
 * 差了 25 倍，没有任何电阻能救它，必须换一种思路：用引脚去"开关"MOSFET。
 */
